VERSION=%%VSN%%

include common.mk

clean: common_clean

test: compile
	$(MAKE) -C test VERSION=$(VERSION)
	$(LIBS_PATH) erl -pa ebin test -eval \
	    'error_logger:tty(false), io:format("Testing in progress. Please wait...~n"), network_client_SUITE:test(), halt()'
